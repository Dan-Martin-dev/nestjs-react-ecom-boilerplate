{
  "name": "@repo/db",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "prisma": {
    "schema": "./schema.prisma"
  },
  "scripts": {
    "db:generate": "dotenv -e ../../.env.dev -- prisma generate",
    "db:migrate": "dotenv -e ../../.env.dev -- prisma migrate dev",
    "db:push": "dotenv -e ../../.env.dev -- prisma db push",
    "db:studio": "dotenv -e ../../.env.dev -- prisma studio",
    "db:reset": "dotenv -e ../../.env.dev -- prisma migrate reset",
    "db:deploy": "dotenv -e ../../.env.prod -- prisma migrate deploy",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "dotenv-cli": "^8.0.0",
    "prisma": "^5.22.0",
    "typescript": "^5.4.5"
  }
}
