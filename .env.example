# Shared Environment Variables (used by multiple apps/services)
DATABASE_URL="postgresql://user:password@localhost:5432/ecommerce"
DOMAIN=localhost
ACME_EMAIL=dev@localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=monorepo-ecom-dev
JWT_SECRET=devjwtsecretkey
JWT_REFRESH_SECRET=devjwtrefreshkey
BCRYPT_SALT_ROUNDS=10
GRAFANA_PASSWORD=devgrafana
NOTIFICATION_EMAIL=dev@localhost
HETZNER_SERVER_IP=127.0.0.1
SMTP_USER=
SMTP_PASS=
S3_BACKUP_BUCKET=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# App-specific variables are in respective app .env files:
# - apps/api/.env (PORT, CORS_ORIGIN, OAuth configs)
# - apps/web/.env (VITE_API_URL)
ORACLE_ENDPOINT="https://<namespace>.objectstorage.<region>.oraclecloud.com"
ORACLE_REGION="us-ashburn-1"
ORACLE_ACCESS_KEY="your_oracle_access_key"
ORACLE_SECRET_KEY="your_oracle_secret_key"
ORACLE_BUCKET="your_bucket_name"

# CDN (Cloudflare)
CDN_HOST="https://images.yourdomain.com"

# Other env vars...